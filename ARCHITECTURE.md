# 🎙️ Voicebox Qwen Web UI - 核心架构深度导读

这份文档专为这套系统的管理员和二次开发者准备。我们尽量不用高深莫测的专业晦涩词汇，而是用**通俗易懂的“大白话”加上生动的比喻**，带你从前到后、从里到外把这套系统彻底看透。

---

## 🏗️ 整体长什么样？(宏观架构)

这套系统像是一家拥有**接待大厅（前端网页）**、**总控台（后端路由）**和**黑箱生产车间（GPU和模型）**的现代化工厂。

- **前台接待（`static/` 里的 `index.html` + `app.js`）**：
  这里纯粹用最基础的 HTML 和原生的 JavaScript 写的，没有用 Vue 或 React 等复杂框架。相当于用砖头直接砌的墙，非常轻快。所有的交互（点按钮、弹窗、播放声音）都在这里发生。
  
- **中枢总管（`backend/main.py`）**：
  它是这家工厂的“厂长”，用 **FastAPI** 编写。它负责核对前台来客的身份信息（X-User-Name 头），管理小仓库（SQLite 数据库），并且负责把客人的“生产诉求”整理好，排成队，塞进车间门缝底下的**任务队列（Queue）**里。
  
- **发音车间（Qwen3-TTS 大模型加载区）**：
  我们在后台单独开辟了一个属于 GPU 的无限死循环函数 `bg_gpu_worker`，它就像一个在流水线上永远不关机的老练车间主任。厂长把活儿塞进队列，它就默默一件一件拿出来干，干完一件，就把生成好的音频文件放回硬盘，然后按响铃铛（状态标记为 done）通知厂长。

---

## 🕵️‍♂️ 多账号隔离逻辑 (他们是怎么做到互不干扰的？)

既然是一个对外开放的公网版网站，最大的痛点就是：**怎么保证张三看不到李四捏的专属情人音色，也听不见李四昨天晚上用来合成的小剧场录音？**

### 1. 简易但牢靠的“工牌认证”
- 当用户第一次打开网页，会被一扇黑屏大门（登录弹窗）拦住。
- 输入诸如 `zhangsan` 这样的名字后，系统并不校验什么复杂的密码，而是直接在你的浏览器（LocalStorage）里发一张写着 `zhangsan` 的电子工牌。
- 后续无论你点什么按钮，发什么请求到后端，JS 代码都会像安检一样的**强制把这张工牌夹在信封表面（HTTP Header: `X-User-Name`）**上。

### 2. 数据库里的虚拟“地盘”
后端 SQLite 数据库（`voicebox_local.db`）有三张核心大表：
- **`users` (户口本)**：登记了访问过服务器的所有名字。如果你被标记成了 `admin` 角色，你就是上帝，可以无视大部分隔离墙。
- **`profiles` (资产房)**：存放**提取好的音色**。里面有一个非常关键的字段叫 `owner`。当 `zhangsan` 访问网页想要获取音色列表时，系统只会执行 `SELECT * WHERE owner = 'zhangsan'`，把别人的东西藏得死死的！
- **`history` (明信片)**：存放**所有的生成历史**。里面也强制记录了 `username`。同理，查询记录时也只会看属于自己的名字。

### 3. “零拷贝”的全局资源池魔法
如果我们有三个做得特别好、谁都可以用的模板音色（Lauren, WJQ, 男声01），如果我们要在100个用户账户里每个人复制一份音频源文件发给他们，硬盘当场就会爆炸！
- **魔法解法：** 我们将这三个极品的 `owner` 设置为 `system`（系统），并把 `is_default` 标记为 `1`。
- **派发逻辑：** 当普通用户 `zhangsan` 索要音色列表时，他不但能拿到自己练出来的私有音色，还会附赠拿到系统判定为 `is_default=1` 的公用大锅饭音色。**实际在硬盘上，这几个音频文件只有独存的一份！**

---

## 🚥 神奇的“排队系统”和“无限推流”

### 为什么要排队？（阻塞与高并发的矛盾）
合成音频非常耗费 GPU！12G 显存的显卡同一时间也许只能伺候1~2个请求。如果此时 5 个人同时点击“生成语音”，如果我们不管，同时把这5个任务丢进模型里运算，显存瞬间就会像吹爆的气球一样触发 `CUDA Out of Memory` 崩溃。

**如何解决？—— 叫号机体系 (`asyncio.Queue`)**
- 厂长（FastAPI API）**绝对不去碰 AI 模型代码**，他只负责把客户的需求打包成一个字典 `task = {'text': '你好', ...}`，然后发配到系统内存的一个**管子（Job Queue）**里。
- 客户（前端网页）随后就会因为 `while True` 循环被原地“挂机”在这里不停地打转，死死盯着自己提交的任务状态。
- **黑箱车间 (`bg_gpu_worker`)**，这是一个永远在后台运行的独立协程。它就像一个有条不紊的老师傅，从管子里拿出一个需求，调用显卡算完、保存变成 WAV 后，再拿下一个！它永远不会分屏去同时做两单，因此完美保证了显卡的百分百安全！
- 算完一个，它就在总登记簿 (`job_statuses`) 里把这单写成 `"status": "done"`。
- 一直在打着圈圈焦急等待的前端一看见状态变成了 "done"，马上领走合成好的音频退出挂机态。

### 长文本的终极解药：SSE (Server-Sent Events) 单向广播
如果你写了洋洋洒洒 500 字，按常规逻辑，你需要坐在电脑前等上 1 分钟，直到一整个 1 分钟的完整大录音生成完毕，你才能听到第一个字。这会急死人。

**SSE 流式推送魔法 (`/api/generate_stream`)：**
- 遇到超过 50 字的长文本，前台会毫不犹豫地开启**流水线模式**。
- 后端依然进入模型，但是后端不是把整个大音频算完再给反馈。而是模型只要吃完一小句（根据标点符号分割），比如“你好啊。”，它就会立刻把这一小段仅有 1 秒钟的短音频，转化为一串文字密码（Base64），通过跟前台建立的一根特殊的“持久水管（SSE流）”，直接“吐”出去发到客户端浏览器！
- 客户端的 JS 代码里，有一个叫 `audioQueue` 的内部管子，上面只要吐下来一段声音，JS 就立刻塞进网页喇叭里播放。就这样，它一边运算一边播放，让你甚至感受不到 1 分钟的总等待时间！

---

## 🛜 部署与生命力护盾 (Cloudflare + SakuraFrp)

作为一个家庭宽带搭建出海给多人的服务，我们如何绕开三大运营商那烦死人的端口封堵？
1. **穿针引线（SakuraFrp 内网穿透）**：我们在你的 Ubuntu 电脑里放了一个叫 `frpc` 的小程序。这个小程序就像一个深海潜水员里的氧气探头，悄咪咪在内网建立了一条长连接直通“新加坡/香港”的云服务器节点。外部的合法请求，完全是通过这个节点“倒灌”进家里的服务器。
2. **障眼法门（Cloudflare 灰云解析）**：我们在外网并不直接买昂贵的服务器，而是白嫖了互联网最大保护伞 Cloudflare 的 DNS 解析权。让 `amorwest.cn` 秒级指向樱花给的节点地址。
3. **绿色锁头（ACME 的签发艺术）**：为了让各端的 Chrome 和 Safari 手机浏览器愿意把“麦克风录音权限”痛快地交给你并杜绝报警，我们用 ACME 手动考取了这颗域名的加密证书放置在根目录，后端在 `run.sh` 时通过套入 `--ssl-keyfile` 直接化身为顶级的、不会报红色警告的全加密 `HTTPS` 安全金库。

---

**这，就是 Voicebox 为什么在任何残苛恶劣环境下，依然能流畅、安全运行的架构底蕴！**
